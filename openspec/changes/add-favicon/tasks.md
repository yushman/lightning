# Tasks

- [x] 1. Embed SVG favicon, serve at /favicon.svg and /favicon.ico, add `<link rel="icon">` to the page layout (landing page got the same icon inline)
- [x] 2. E2E: curl both routes → 200 image/svg+xml; page HTML contains the link tag; gates green
